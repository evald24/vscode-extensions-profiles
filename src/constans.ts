// Platform slash windows "\" or linux and darwin "/"
export const PLATFORM_SLASH = process.platform === "win32" ? "\\" : "/";

// User should not be able to update a profile with this name
export const GLOBAL_PROFILE = "Global Profile";
